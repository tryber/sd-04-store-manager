const validationsProducts = require('./prodValidations');
const salesValidations = require('./salesValidations');
const errors = require('./errors');

module.exports = {
  validationsProducts,
  errors,
  salesValidations,
};
